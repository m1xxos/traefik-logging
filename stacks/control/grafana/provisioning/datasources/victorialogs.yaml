apiVersion: 1

# Only answers while backend=victorialogs is up; the plugin is installed via
# GF_INSTALL_PLUGINS in compose.yaml.
datasources:
- name: VictoriaLogs
  uid: VictoriaLogs
  type: victoriametrics-logs-datasource
  access: proxy
  url: http://192.168.1.63:9428
  editable: false
