apiVersion: 1

# Only answers while backend=loki is up. Any node accepts queries in cluster
# mode, so backend-1 is fine for both modes.
datasources:
- name: Loki
  uid: Loki
  type: loki
  access: proxy
  url: http://192.168.1.63:3100
  editable: false
